[paket]
tanim   = 3D wayland pencere yöneticisi
paketci = milisarge
grup    = sistem
url     = https://wayfire.org

[gerek]
derleme = meson wayland wayland-protocols pango libjpeg-turbo glm nlohmann-json mesa libinput libxkbcommon vulkan glslang seatd xcb-util-wm xcb-util-image xcb-util-errors xcb-util-renderutil hwids libdisplay-info libliftoff
calisma = 

[kaynak]
git     = https://github.com/WayfireWM/wayfire
git     = https://github.com/WayfireWM/wf-config
git     = https://github.com/WayfireWM/wf-utils
git     = https://github.com/WayfireWM/wf-touch
5       = https://gitlab.freedesktop.org/wlroots/wlroots/-/releases/0.17.4/downloads/wlroots-0.17.4.tar.gz

[derle]
betik   = rmdir subprojects/*
betik   = cp -r /sources/wf-config subprojects/wf-config
;cd subprojects/wf-config;git checkout 5cd0c8a;cd -
betik   = cp -r /sources/wf-utils subprojects/wf-utils
;cd subprojects/wf-utils;git checkout 15f8e16;cd -
betik   = cp -r /sources/wf-touch subprojects/wf-touch
;cd subprojects/wf-touch;git checkout b8b844f;cd -
betik   = mv $SRC/wlroots-* subprojects/wlroots
ekconf  = -Dtests=disabled -Dxwayland=disabled -Duse_system_wfconfig=disabled -Duse_system_wlroots=disabled -Dwf-touch:tests=disabled -Dwf-config:tests=disabled
tip     = meson

[pakur]
tip     = meson
betik   = install -Dm644 $isim-$surum/wayfire.desktop "${PKG}/usr/share/wayland-sessions/wayfire.desktop"
betik   = install -Dm644 $isim-$surum/wayfire.ini "${PKG}/etc/wayfire/wayfire.ini"
betik   = rm -rf $PKG/usr/man
betik   = rm -rf $PKG/usr/lib/*.a
